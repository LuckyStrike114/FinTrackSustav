<Window x:Class="FinTrackSustav.AddTransactionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Dodaj transakciju" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Unos opisa transakcije -->
        <Label Content="Opis:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
        <TextBox x:Name="txtDescription" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <!-- Unos iznosa transakcije -->
        <Label Content="Iznos:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
        <TextBox x:Name="txtAmount" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- Odabir datuma -->
        <Label Content="Datum:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
        <DatePicker x:Name="dpDate" Grid.Row="2" Grid.Column="1" Margin="5" SelectedDate="{x:Static sys:DateTime.Now}"/>

        <!-- Tipka Spremi i Odustani -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Spremi" Width="75" Margin="5" Click="BtnSave_Click"/>
            <Button Content="Odustani" Width="75" Margin="5" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
